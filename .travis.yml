language: ruby
rvm:
  - 2.5.0
before_install:
  - gem install bundler       
  - bundle install
  - bundle update
  - gem update bundler

install:
  - gem install rails
script:
 — bundle install — jobs=3 — retry=3
 — bundle exec rake db:create
 — bundle exec rake db:migrate
 — bundle exec rspec