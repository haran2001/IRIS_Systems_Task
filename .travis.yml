language: ruby

before_install:
  - rvm install ruby-railties
  - gem install bundler
  - gem update bundler

install:
  - gem install rails

script:
  - gem install bundler
  - gem update bundler
  - bundle config set deployment 'true'
  - rails test test


