language: ruby

# services:
#   database:
#     - postgresql
before_install:
  - rvm install ruby-railties
  - rvm install 2.6.1
  - rvm use 2.6.1
  - gem install bundler
  - bundle install
  - bundle update
  - gem update bundler


install:
  - gem install rails

script:
  - bundle config set deployment 'true'
  - rails test test


